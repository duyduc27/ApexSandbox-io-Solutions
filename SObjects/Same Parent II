public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if (con.AccountId == acc.Id && opp.AccountId == acc.Id && !String.isBlank(acc.Id) ){
        return true;
    } else {
        return false;
    }
}